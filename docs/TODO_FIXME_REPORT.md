# TODO/FIXME Report

Generated by `tools/generate_todo_report.py`.

Summary:

- **complex**: 17 items
- **implementable**: 76 items
- **unknown**: 78 items

---

## Complex (17)

- **FIXME** in `launcher/ResourceDownloadTask.cpp`:72 — // FIXME: Daha generic hale getirilmeli. IndexedVersion'a ek bilgi eklenmesi gerekebilir.
```
    QDir dir{ m_pack_model->dir() };
    {
        // FIXME: Daha generic hale getirilmeli. IndexedVersion'a ek bilgi eklenmesi gerekebilir.
        //        or adquiring a reference to the base instance.
        if (!m_custom_target_folder.isEmpty()) {
```

- **FIXME** in `launcher/InstanceList.h`:227 — // FIXME: this is so inefficient that looking at it is almost painful.
```
    QString m_instDir;
    QFileSystemWatcher* m_watcher;
    // FIXME: this is so inefficient that looking at it is almost painful.
    QSet<QString> m_collapsedGroups;
    QMap<InstanceId, GroupId> m_instanceGroupIndex;
```

- **TODO** in `systeminfo/src/sys_apple.cpp`:18 — // TODO: figure out how to detect cursed-ness (macOS emulated on linux via mad hacks and so on)
```
    QString release = out.kernelVersion = buf.release;

    // TODO: figure out how to detect cursed-ness (macOS emulated on linux via mad hacks and so on)
    out.isCursed = false;

```

- **TODO** in `launcher/tasks/Task.h`:224 — // TODO: Nuke in favor of QLoggingCategory
```
    int m_progressTotal = 100;

    // TODO: Nuke in favor of QLoggingCategory
    bool m_show_debug = true;

```

- **FIXME** in `launcher/meta/VersionList.cpp`:100 — // FIXME: HACK: this should be generic and be replaced by something else. Anything that is a hard 'equals' dep is a 'parent
```
            return version->version();
        case ParentVersionRole: {
            // FIXME: HACK: this should be generic and be replaced by something else. Anything that is a hard 'equals' dep is a 'parent
            // uid'.
            auto& reqs = version->requiredSet();
```

- **FIXME** in `launcher/minecraft/LaunchProfile.cpp`:384 — // FIXME: HACK!! jar modding is weird and unsystematic!
```
    // NOTE: order is important here, add main jar last to the lists
    if (m_mainJar) {
        // FIXME: HACK!! jar modding is weird and unsystematic!
        if (m_jarMods.size()) {
            QDir tempDir(tempPath);
```

- **FIXME** in `launcher/minecraft/PackProfile.cpp`:352 — // FIXME: differentiate when a reapply is required by propagating state from components
```
    saveNow();

    // FIXME: differentiate when a reapply is required by propagating state from components
    invalidateLaunchProfile();

```

- **FIXME** in `launcher/minecraft/PackProfile.cpp`:765 — // FIXME: we need a generic way of removing local resources, not just jar mods...
```
    }

    // FIXME: we need a generic way of removing local resources, not just jar mods...
    auto preRemoveJarMod = [this](LibraryPtr jarMod) -> bool {
        if (!jarMod->isLocal()) {
```

- **TODO** in `launcher/minecraft/Component.h`:98 — // DEPRECATED: explicit numeric order values, used for loading old non-component config. TODO: refactor and move to migration code
```
    QStringList knownConflictingComponents();

    // DEPRECATED: explicit numeric order values, used for loading old non-component config. TODO: refactor and move to migration code
    void setOrder(int order);
    int getOrder();
```

- **TODO** in `launcher/minecraft/Component.h`:161 — // DEPRECATED: explicit numeric order values, used for loading old non-component config. TODO: refactor and move to migration code
```
    // END: persistent component list properties

    // DEPRECATED: explicit numeric order values, used for loading old non-component config. TODO: refactor and move to migration code
    bool m_orderOverride = false;
    int m_order = 0;
```

- **FIXME** in `launcher/minecraft/ComponentUpdateTask.cpp`:47 — * FIXME: the 'one shot async task' nature of this does not fit the intended usage
```

/*
 * FIXME: the 'one shot async task' nature of this does not fit the intended usage
 * Really, it should be a reactor/state machine that receives input from the application
 * and dynamically adapts to changing requirements...
```

- **FIXME** in `launcher/minecraft/update/AssetUpdateTask.cpp`:84 — // FIXME: this looks like a job for a generic validator based on json schema?
```

    QString asset_fname = "assets/indexes/" + assets->id + ".json";
    // FIXME: this looks like a job for a generic validator based on json schema?
    if (!AssetsUtils::loadAssetsIndexJson(assets->id, asset_fname, index)) {
        auto metacache = APPLICATION->metacache();
```

- **TODO** in `launcher/modplatform/flame/FlameInstanceCreationTask.cpp`:741 — // TODO: Diğer kaynak tipleriyle de çalışacak şekilde genişletilmeli.
```
        }
    }
    // TODO: Diğer kaynak tipleriyle de çalışacak şekilde genişletilmeli.
    auto task = makeShared<ConcurrentTask>("CreateModMetadata", APPLICATION->settings()->get("NumberOfConcurrentTasks").toInt());
    auto results = m_modIdResolver->getResults().files;
```

- **TODO** in `launcher/ui/widgets/PageContainer_p.h`:78 — // HACK: fixes icon stretching on windows. TODO: report Qt bug for this
```
                if (icon.isNull())
                    icon = m_emptyIcon;
                // HACK: fixes icon stretching on windows. TODO: report Qt bug for this
                return QIcon(icon.pixmap(QSize(48, 48)));
            }
```

- **FIXME** in `launcher/ui/instanceview/InstanceDelegate.cpp`:276 — // FIXME: Bu kodun burada olmaması gerekiyor. Daha generic bir yapıya taşınmalı.
```
    }

    // FIXME: Bu kodun burada olmaması gerekiyor. Daha generic bir yapıya taşınmalı.
    auto instance = (BaseInstance*)index.data(InstanceList::InstancePointerRole).value<void*>();
    if (instance) {
```

- **TODO** in `launcher/ui/pages/instance/ManagedPackPage.h`:133 — // TODO: We may want to show this page with some useful info at some point.
```
    ~GenericManagedPackPage() override = default;

    // TODO: We may want to show this page with some useful info at some point.
    bool shouldDisplay() const override { return false; };
};
```

- **FIXME** in `launcherjava/tr/org/projecttick/projtlauncher/legacy/LegacyFrame.java`:181 — // FIXME better solution
```
        @Override
        public void windowClosing(WindowEvent event) {
            // FIXME better solution

            new Thread(new Runnable() {
```

## Implementable (76)

- **TODO** in `CMakeLists.txt`:119 — # TODO: Look into -gc-sections to further reduce binary size
```
        # -ffunction-sections and -fdata-sections help reduce binary size
        # -mguard=cf enables Control Flow Guard
        # TODO: Look into -gc-sections to further reduce binary size
        foreach(lang C CXX)
            set("CMAKE_${lang}_FLAGS_RELEASE" "-ffunction-sections -fdata-sections -mguard=cf")
```

- **TODO** in `update.sh`:78 — launcher_git add org.quiltmc.quilt-loader/* || fail_out # TODO: add Quilt hashed, once it is actually used
```
    launcher_git add net.neoforged/* || fail_out
    launcher_git add net.fabricmc.fabric-loader/* net.fabricmc.intermediary/* || fail_out
    launcher_git add org.quiltmc.quilt-loader/* || fail_out # TODO: add Quilt hashed, once it is actually used
    launcher_git add com.mumfrey.liteloader/* || fail_out
    launcher_git add net.minecraft.java/* net.adoptium.java/* com.azul.java/* || fail_out
```

- **TODO** in `launcher/FileSystem.h`:163 — // TODO: maybe add a "shouldCopy" signal in the future?
```
    void fileCopied(const QString& relativeName);
    void copyFailed(const QString& relativeName);
    // TODO: maybe add a "shouldCopy" signal in the future?

   private:
```

- **FIXME** in `launcher/CMakeLists.txt`:840 — # FIXME: maybe find a better home for this.
```
    ui/ViewLogWindow.cpp

    # FIXME: maybe find a better home for this.
    FileIgnoreProxy.cpp
    FileIgnoreProxy.h
```

- **FIXME** in `launcher/BaseInstance.h`:245 — /// FIXME: this really should be elsewhere...
```
    virtual QString getStatusbarDescription() = 0;

    /// FIXME: this really should be elsewhere...
    virtual QString instanceConfigFolder() const = 0;

```

- **TODO** in `launcher/InstanceImportTask.cpp`:331 — // TODO: Doğrudan import edilen ZIP dosyalarından instance ID'leri alınabilmeli. Şu anda ID'ler eksik kalıyor.
```
            makeShared<FlameCreationTask>(m_stagingPath, m_globalSettings, nullptr, pack_id, pack_version_id, original_instance_id);
    } else {
        // TODO: Doğrudan import edilen ZIP dosyalarından instance ID'leri alınabilmeli. Şu anda ID'ler eksik kalıyor.
        QString pack_id;
        QString pack_version_id;
```

- **TODO** in `launcher/InstanceImportTask.cpp`:453 — // TODO: Doğrudan import edilen ZIP dosyalarından instance ID'leri alınabilmeli. Şu anda ID'ler eksik kalıyor.
```
        }

        // TODO: Doğrudan import edilen ZIP dosyalarından instance ID'leri alınabilmeli. Şu anda ID'ler eksik kalıyor.
        inst_creation_task = makeShared<ModrinthCreationTask>(m_stagingPath, m_globalSettings, nullptr, pack_id);
    }
```

- **TODO** in `launcher/Application.cpp`:482 — // TODO: Multiple instances with different data dirs can run from same binary path
```
    auto appID = ApplicationId::fromPathAndVersion(QDir::currentPath(), BuildConfig.printableVersionString());
    {
        // TODO: Multiple instances with different data dirs can run from same binary path
        // This can cause update conflicts - consider using data path in ApplicationId
        m_peerInstance = new LocalPeer(this, appID);
```

- **FIXME** in `launcher/VersionProxyModel.cpp`:109 — // FIXME: implement when needed
```
    connect(filterModel, &QAbstractItemModel::rowsAboutToBeRemoved, this, &VersionProxyModel::sourceRowsAboutToBeRemoved);
    connect(filterModel, &QAbstractItemModel::rowsRemoved, this, &VersionProxyModel::sourceRowsRemoved);
    // FIXME: implement when needed
    /*
    connect(replacing, &QAbstractItemModel::rowsAboutToBeMoved, this, &VersionProxyModel::sourceRowsAboutToBeMoved);
```

- **FIXME** in `launcher/VersionProxyModel.cpp`:134 — return tr("Minecraft");  // FIXME: this should come from metadata
```
                return tr("Version");
            case ParentVersion:
                return tr("Minecraft");  // FIXME: this should come from metadata
            case Branch:
                return tr("Branch");
```

- **FIXME** in `launcher/VersionProxyModel.cpp`:155 — return tr("Minecraft version");  // FIXME: this should come from metadata
```
                return tr("The name of the version.");
            case ParentVersion:
                return tr("Minecraft version");  // FIXME: this should come from metadata
            case Branch:
                return tr("The version's branch");
```

- **TODO** in `launcher/InstanceCopyTask.cpp`:51 — // TODO: Kopyalanan instance'ın tipini orijinal instance'dan almak gerekiyor. Şu anda sabit tip atanıyor.
```
    if (!filters.isEmpty()) {
        // Set regex filter:
        // TODO: Kopyalanan instance'ın tipini orijinal instance'dan almak gerekiyor. Şu anda sabit tip atanıyor.
        QRegularExpression regexp(filters, QRegularExpression::CaseInsensitiveOption);
        m_matcher = Filters::regexp(regexp);
```

- **todo** in `tools/generate_todo_report.py`:49 — if 'should' in t or 'maybe' in t or 'add' in t or 'implement' in t or 'todo:' in t:
```
        return 'complex'
    # if contains words like 'should' or 'maybe' consider implementable
    if 'should' in t or 'maybe' in t or 'add' in t or 'implement' in t or 'todo:' in t:
        return 'implementable'
    return 'unknown'
```

- **TODO** in `meta/model/__init__.py`:242 — exclude: List[str]  # TODO maybe drop this completely?
```
    """

    exclude: List[str]  # TODO maybe drop this completely?


```

- **TODO** in `meta/model/mojang.py`:274 — id: str  # TODO: optional?
```
        return v

    id: str  # TODO: optional?
    arguments: Optional[MojangArguments]
    asset_index: Optional[MojangAssets] = Field(alias="assetIndex")
```

- **TODO** in `meta/model/mojang.py`:278 — downloads: Optional[Dict[str, MojangArtifactBase]]  # TODO improve this?
```
    asset_index: Optional[MojangAssets] = Field(alias="assetIndex")
    assets: Optional[str]
    downloads: Optional[Dict[str, MojangArtifactBase]]  # TODO improve this?
    libraries: Optional[List[Library]]  # TODO: optional?
    main_class: Optional[str] = Field(alias="mainClass")
```

- **TODO** in `meta/model/mojang.py`:279 — libraries: Optional[List[Library]]  # TODO: optional?
```
    assets: Optional[str]
    downloads: Optional[Dict[str, MojangArtifactBase]]  # TODO improve this?
    libraries: Optional[List[Library]]  # TODO: optional?
    main_class: Optional[str] = Field(alias="mainClass")
    applet_class: Optional[str] = Field(alias="appletClass")
```

- **TODO** in `meta/model/mojang.py`:324 — ):  # some versions don't have this. TODO: maybe maintain manual overrides
```
        if (
            self.javaVersion is not None
        ):  # some versions don't have this. TODO: maybe maintain manual overrides
            major = self.javaVersion.major_version
            javaName = self.javaVersion.component
```

- **TODO** in `meta/run/update_forge.py`:364 — # TODO: fix those versions?
```
        else:
            # ignore the two versions without install manifests and jar mod class files
            # TODO: fix those versions?
            if version.mc_version_sane == "1.6.1":
                continue
```

- **TODO** in `meta/run/update_neoforge.py`:183 — # TODO: what *is* recommended?
```
            continue

        # TODO: what *is* recommended?
        is_recommended = False

```

- **todo** in `website/admin/config.yml`:94 — # todo: add support for news and the W.I.P themes page.
```
      - { label: "Body", name: "body", widget: "markdown" }

# todo: add support for news and the W.I.P themes page.
```

- **TODO** in `website/wiki/development/build-instructions/windows.md`:124 — <!-- TODO: The VS-Code instructions could be done better for windows (and in general) -->
```
You can [download it here](https://ccache.dev/download.html). After setting up, builds will be incremental, and the builds after the first one will be much faster.

<!-- TODO: The VS-Code instructions could be done better for windows (and in general) -->

### VS Code
```

- **FIXME** in `launcher/launch/LaunchTask.cpp`:228 — // FIXME: should this really be here?
```
        m_logModel->setMaxLines(getConsoleMaxLines(m_instance->settings()));
        m_logModel->setStopOnOverflow(shouldStopOnConsoleOverflow(m_instance->settings()));
        // FIXME: should this really be here?
        m_logModel->setOverflowMessage(tr("Stopped watching the game log because the log length surpassed %1 lines.\n"
                                          "You may have to fix your mods because the game is still logging to files and"
```

- **FIXME** in `launcher/meta/VersionList.cpp`:131 — // FIXME: this should be determined in whatever view/proxy is used...
```
            return major;
        }
        // FIXME: this should be determined in whatever view/proxy is used...
        // case LatestRole: return version == getLatestStable();
        default:
```

- **TODO** in `launcher/meta/VersionList.cpp`:260 — // TODO: do not reset the whole model. maybe?
```
    }

    // TODO: do not reset the whole model. maybe?
    beginResetModel();
    if (other->m_versions.isEmpty()) {
```

- **TODO** in `launcher/icons/MMCIcon.cpp`:106 — // TODO: Theme ikonları için dependency injection uygulanabilir. Şu anda doğrudan erişim var, test edilebilirlik ve özelleştirme için DI önerilir.
```
    if (!icon.isNull())
        return icon;
    // TODO: Theme ikonları için dependency injection uygulanabilir. Şu anda doğrudan erişim var, test edilebilirlik ve özelleştirme için DI önerilir.
    return QIcon::fromTheme(m_images[m_current_type].key);
}
```

- **todo** in `launcher/minecraft/PackProfile.h`:175 — // todo(merged): is this the best approach
```

    /// Add the component to the internal list of patches
    // todo(merged): is this the best approach
    void appendComponent(ComponentPtr component);

```

- **TODO** in `launcher/minecraft/Library.h`:87 — /// limited copy without some data. TODO: why?
```
    Library() {}
    Library(const QString& name) { m_name = name; }
    /// limited copy without some data. TODO: why?
    static LibraryPtr limitedCopy(LibraryPtr base)
    {
```

- **FIXME** in `launcher/minecraft/VersionFilterData.cpp`:76 — // FIXME: remove, used for deciding when core mods should display
```
    forgeInstallerBlacklist = QSet<QString>({ "1.5.2" });

    // FIXME: remove, used for deciding when core mods should display
    legacyCutoffDate = timeFromS3Time("2013-06-25T15:08:56+02:00");
    lwjglWhitelist = QSet<QString>{ "net.java.jinput:jinput", "net.java.jinput:jinput-platform", "net.java.jutils:jutils",
```

- **FIXME** in `launcher/minecraft/LaunchProfile.cpp`:273 — // FIXME: implement something that actually makes sense here
```
const QList<PatchProblem> LaunchProfile::getProblems() const
{
    // FIXME: implement something that actually makes sense here
    return {};
}
```

- **TODO** in `launcher/minecraft/PackProfile.cpp`:624 — // TODO: this will need fine-tuning later...
```

    auto patch = d->components.at(row);
    // TODO: this will need fine-tuning later...
    if (patch->canBeDisabled() && !d->interactionDisabled) {
        outFlags |= Qt::ItemIsUserCheckable;
```

- **TODO** in `launcher/minecraft/PackProfile.cpp`:1056 — // TODO: remove this or add version condition once Quilt drops official Fabric support
```
        return loadersOpt;
    auto loaders = loadersOpt.value();
    // TODO: remove this or add version condition once Quilt drops official Fabric support
    if (loaders & ModPlatform::Quilt)
        loaders |= ModPlatform::Fabric;
```

- **TODO** in `launcher/minecraft/PackProfile.cpp`:1073 — // TODO: remove this or add version condition once Quilt drops official Fabric support
```
    }

    // TODO: remove this or add version condition once Quilt drops official Fabric support
    if (result.contains(ModPlatform::Quilt) && !result.contains(ModPlatform::Fabric)) {
        result.append(ModPlatform::Fabric);
```

- **TODO** in `launcher/minecraft/MinecraftInstance.cpp`:321 — // TODO: move UI code out of MinecraftInstance
```
}

// TODO: move UI code out of MinecraftInstance
void MinecraftInstance::populateLaunchMenu(QMenu* menu)
{
```

- **FIXME** in `launcher/minecraft/ComponentUpdateTask.cpp`:445 — // FIXME, TODO: decouple dependency resolution from loading
```
}

// FIXME, TODO: decouple dependency resolution from loading
// FIXME: This works directly with the PackProfile internals. It shouldn't! It needs richer data types than PackProfile uses.
// FIXME: throw all this away and use a graph
```

- **FIXME** in `launcher/minecraft/ComponentUpdateTask.cpp`:446 — // FIXME: This works directly with the PackProfile internals. It shouldn't! It needs richer data types than PackProfile uses.
```

// FIXME, TODO: decouple dependency resolution from loading
// FIXME: This works directly with the PackProfile internals. It shouldn't! It needs richer data types than PackProfile uses.
// FIXME: throw all this away and use a graph
void ComponentUpdateTask::resolveDependencies(bool checkOnly)
```

- **FIXME** in `launcher/minecraft/ComponentUpdateTask.cpp`:447 — // FIXME: throw all this away and use a graph
```
// FIXME, TODO: decouple dependency resolution from loading
// FIXME: This works directly with the PackProfile internals. It shouldn't! It needs richer data types than PackProfile uses.
// FIXME: throw all this away and use a graph
void ComponentUpdateTask::resolveDependencies(bool checkOnly)
{
```

- **FIXME** in `launcher/minecraft/ComponentUpdateTask.cpp`:552 — // FIXME: this should not work directly with the component list
```
            }
            component->m_dependencyOnly = true;
            // FIXME: this should not work directly with the component list
            d->m_profile->insertComponent(add.indexOfFirstDependee, component);
            componentIndex[add.uid] = component;
```

- **FIXME** in `launcher/minecraft/ComponentUpdateTask.cpp`:561 — // FIXME: this should not work directly with the component list
```
        // change a version of something that exists
        for (auto& change : toChange) {
            // FIXME: this should not work directly with the component list
            qCDebug(instanceProfileResolveC) << "Setting version of " << change.uid << "to" << change.equalsVersion;
            auto component = componentIndex[change.uid];
```

- **TODO** in `launcher/minecraft/auth/Parsers.cpp`:89 — // TODO: handle error responses ...
```
 }
*/
// TODO: handle error responses ...
/*
{
```

- **TODO** in `launcher/minecraft/auth/AccountList.h`:78 — // TODO: Add icon column.
```

    enum VListColumns {
        // TODO: Add icon column.
        ProfileNameColumn = 0,
        NameColumn,
```

- **TODO** in `launcher/minecraft/auth/MinecraftAccount.h`:181 — // TODO: better signalling for the various possible state changes - especially errors
```
    void activityChanged(bool active);

    // TODO: better signalling for the various possible state changes - especially errors

   protected: /* variables */
```

- **FIXME** in `launcher/minecraft/launch/MinecraftTarget.cpp`:41 — // FIXME: the way this is written, it can't ever do any sort of validation and can accept total junk
```
#include <QStringList>

// FIXME: the way this is written, it can't ever do any sort of validation and can accept total junk
MinecraftTarget MinecraftTarget::parse(const QString& fullAddress, bool useWorld)
{
```

- **TODO** in `launcher/minecraft/mod/ResourcePack.h`:32 — /* TODO:
```
class Version;

/* TODO:
 *
 * Store localized descriptions
```

- **TODO** in `launcher/minecraft/mod/Resource.cpp`:147 — // TODO: removeThePrefix fonksiyonu gerekli mi? 0 döndürme ihtimali var, test edilmeli.
```
            QString other_name{ other.name() };

            // TODO: removeThePrefix fonksiyonu gerekli mi? 0 döndürme ihtimali var, test edilmeli.
            removeThePrefix(this_name);
            removeThePrefix(other_name);
```

- **TODO** in `launcher/minecraft/mod/DataPack.h`:53 — /* TODO:
```
class Version;

/* TODO:
 *
 * Store localized descriptions
```

- **TODO** in `launcher/minecraft/mod/ResourceFolderModel.h`:78 — *  TODO: Make the resources unique pointers accessible through weak pointers.
```
 *  and the resource's lifetime is contingent on the model's lifetime.
 *
 *  TODO: Make the resources unique pointers accessible through weak pointers.
 */
class ResourceFolderModel : public QAbstractListModel {
```

- **TODO** in `launcher/minecraft/mod/ResourceFolderModel.h`:243 — *  TODO: Figure out a way to express this relationship better without templated classes (Q_OBJECT macro disallows that).
```
     *  This usually calls static_cast on the specific Task type returned by createUpdateTask,
     *  so care must be taken in such cases.
     *  TODO: Figure out a way to express this relationship better without templated classes (Q_OBJECT macro disallows that).
     */
    virtual void onUpdateSucceeded();
```

- **TODO** in `launcher/modplatform/packwiz/Packwiz.cpp`:172 — // TODO: Burada daha fazla işlem yapılmalı, çünkü kullanıcı muhtemelen modun üstüne yazmak istiyor.
```

    // There's already data on there!
    // TODO: Burada daha fazla işlem yapılmalı, çünkü kullanıcı muhtemelen modun üstüne yazmak istiyor.
    // override a file. In this case, check versions and ask the user what
    // they want to do!
```

- **TODO** in `launcher/modplatform/packwiz/Packwiz.h`:97 — * TODO: Ask the user if they want to override, and delete the old mod's files, or keep the old one.
```
    /* Updates the mod index for the provided mod.
     * This creates a new index if one does not exist already
     * TODO: Ask the user if they want to override, and delete the old mod's files, or keep the old one.
     * */
    static void updateModIndex(const QDir& index_dir, Mod& mod);
```

- **todo** in `launcher/modplatform/atlauncher/ATLPackInstallTask.cpp`:1071 — // todo: use more detection
```

    // Use a component to fill in the rest of the data
    // todo: use more detection
    if (!createPackComponent(instance.instanceRoot(), components)) {
        emitFailed(tr("Failed to create pack component"));
```

- **TODO** in `launcher/modplatform/legacy_ftb/PackInstallTask.cpp`:221 — // TODO: Fallback mekanizması eklenmeli. Şu anda hata durumunda sürekli başarısız oluyor.
```

    if (fallback) {
    // TODO: Fallback mekanizması eklenmeli. Şu anda hata durumunda sürekli başarısız oluyor.
        emitFailed(tr("No installation method found!"));
        return;
```

- **TODO** in `launcher/modplatform/flame/FlameInstanceCreationTask.cpp`:192 — // TODO: Currently 'overrides' overwrite everything on update. Unchanged files should be protected.
```

        // We will remove all the previous overrides, to prevent duplicate files!
        // TODO: Currently 'overrides' overwrite everything on update. Unchanged files should be protected.
        auto old_overrides = Override::readOverrides("overrides", old_index_folder);
        for (const auto& entry : old_overrides) {
```

- **TODO** in `launcher/modplatform/modrinth/ModrinthInstanceCreationTask.cpp`:153 — // TODO: Şu anda 'overrides' güncellemede her şeyi ezmekte. Değişmeyen dosyalar korunmalı.
```

        // We will remove all the previous overrides, to prevent duplicate files!
        // TODO: Şu anda 'overrides' güncellemede her şeyi ezmekte. Değişmeyen dosyalar korunmalı.
        // FIXME: Disabled mod'lar için özel bir işlem yapılmalı.
        auto old_overrides = Override::readOverrides("overrides", old_index_folder);
```

- **FIXME** in `launcher/modplatform/modrinth/ModrinthInstanceCreationTask.cpp`:154 — // FIXME: Disabled mod'lar için özel bir işlem yapılmalı.
```
        // We will remove all the previous overrides, to prevent duplicate files!
        // TODO: Şu anda 'overrides' güncellemede her şeyi ezmekte. Değişmeyen dosyalar korunmalı.
        // FIXME: Disabled mod'lar için özel bir işlem yapılmalı.
        auto old_overrides = Override::readOverrides("overrides", old_index_folder);
        for (const auto& entry : old_overrides) {
```

- **TODO** in `launcher/modplatform/modrinth/ModrinthInstanceCreationTask.cpp`:271 — // TODO: Diğer kaynak tipleriyle de çalışacak şekilde genişletilmeli.
```
    auto root_modpack_path = FS::PathCombine(m_stagingPath, m_root_path);
    auto root_modpack_url = QUrl::fromLocalFile(root_modpack_path);
    // TODO: Diğer kaynak tipleriyle de çalışacak şekilde genişletilmeli.
    QHash<QString, Resource*> resources;
    for (auto& file : m_files) {
```

- **TODO** in `launcher/java/download/ManifestDownloadTask.cpp`:117 — // TODO: Eğer varsa sıkıştırılmış versiyonu da indirilmeli. Şu anda sadece normal dosya indiriliyor.
```
            }
        } else if (type == "file") {
            // TODO: Eğer varsa sıkıştırılmış versiyonu da indirilmeli. Şu anda sadece normal dosya indiriliyor.
            auto raw = Json::ensureObject(Json::ensureObject(meta, "downloads"), "raw");
            auto isExec = Json::ensureBoolean(meta, "executable", false);
```

- **TODO** in `launcher/resources/multimc/multimc.qrc`:228 — <!-- and the same again. TODO: make a nice accounts icon -->
```
        <file>48x48/noaccount.png</file>

        <!-- and the same again. TODO: make a nice accounts icon -->
        <file alias="8x8/accounts.png">8x8/noaccount.png</file>
        <file alias="16x16/accounts.png">16x16/noaccount.png</file>
```

- **FIXME** in `launcher/launch/steps/TextPrint.h`:44 — * FIXME: maybe do not export
```

/*
 * FIXME: maybe do not export
 */

```

- **TODO** in `launcher/ui/dialogs/ProgressDialog.cpp`:193 — // TODO: only provide the unique_ptr overloads
```
}

// TODO: only provide the unique_ptr overloads
int ProgressDialog::execWithTask(std::unique_ptr<Task>&& task)
{
```

- **TODO** in `launcher/ui/themes/ThemeManager.cpp`:122 — // TODO: icon themes and instance icons do not mesh well together. Rearrange and fix discrepancies!
```
void ThemeManager::initializeIcons()
{
    // TODO: icon themes and instance icons do not mesh well together. Rearrange and fix discrepancies!
    // set icon theme search path!
    themeDebugLog() << "<> Initializing Icon Themes";
```

- **TODO** in `launcher/ui/themes/ThemeManager.cpp`:174 — // TODO: need some way to differentiate same name themes in different subdirectories
```
    }

    // TODO: need some way to differentiate same name themes in different subdirectories
    //  (maybe smaller grey text next to theme name in dropdown?)

```

- **TODO** in `launcher/ui/themes/ITheme.h`:71 — // TODO: rename to Theme; this is not an interface as it contains method implementations
```
};

// TODO: rename to Theme; this is not an interface as it contains method implementations
// TODO: make methods const
class ITheme {
```

- **TODO** in `launcher/ui/themes/ITheme.h`:72 — // TODO: make methods const
```

// TODO: rename to Theme; this is not an interface as it contains method implementations
// TODO: make methods const
class ITheme {
   public:
```

- **TODO** in `launcher/ui/widgets/ProgressWidget.cpp`:81 — // TODO: should we connect &Task::details
```
    connect(m_task, &Task::finished, this, &ProgressWidget::handleTaskFinish);
    connect(m_task, &Task::status, this, &ProgressWidget::handleTaskStatus);
    // TODO: should we connect &Task::details
    connect(m_task, &Task::progress, this, &ProgressWidget::handleTaskProgress);
    connect(m_task, &Task::destroyed, this, &ProgressWidget::taskDestroyed);
```

- **TODO** in `launcher/ui/widgets/LogView.cpp`:191 — // TODO: some day... maybe
```
void LogView::rowsRemoved(const QModelIndex& parent, int first, int last)
{
    // TODO: some day... maybe
    Q_UNUSED(parent)
    Q_UNUSED(first)
```

- **TODO** in `launcher/ui/instanceview/InstanceView.cpp`:145 — // TODO: for accessibility support, implement+register a factory, steal QAccessibleTable from Qt and return an instance of it for
```
{
    QAbstractItemView::currentChanged(current, previous);
    // TODO: for accessibility support, implement+register a factory, steal QAccessibleTable from Qt and return an instance of it for
    // InstanceView.
#ifndef QT_NO_ACCESSIBILITY
```

- **FIXME** in `launcher/ui/instanceview/AccessibleInstanceView.cpp`:421 — // FIXME: we are in denial of our children. this should stop.
```
                   << iface->text(QAccessible::Name);
    }
    // FIXME: we are in denial of our children. this should stop.
    return -1;
}
```

- **FIXME** in `launcher/ui/pages/instance/VersionPage.h`:124 — /// FIXME: this shouldn't be necessary!
```
    QMenu* createPopupMenu() override;

    /// FIXME: this shouldn't be necessary!
    bool reloadPackProfile();

```

- **FIXME** in `launcher/ui/pages/instance/ManagedPackPage.cpp`:492 — // FIXME: We should display the websiteUrl field, but this requires doing the API request first :(
```
QString FlameManagedPackPage::url() const
{
    // FIXME: We should display the websiteUrl field, but this requires doing the API request first :(
    return "https://www.curseforge.com/projects/" + m_inst->getManagedPackID();
}
```

- **TODO** in `launcher/ui/pages/instance/ScreenshotsPage.cpp`:562 — // TODO: mass renaming
```
        return;
    ui->listView->edit(selection[0]);
    // TODO: mass renaming
}

```

- **TODO** in `launcher/ui/pages/global/LauncherPage.cpp`:236 — // TODO: Offer to move instances to new instance folder.
```

    // Folders
    // TODO: Offer to move instances to new instance folder.
    s->set("InstanceDir", ui->instDirTextBox->text());
    s->set("CentralModsDir", ui->modsDirTextBox->text());
```

- **TODO** in `launcher/ui/pages/modplatform/flame/FlamePage.cpp`:218 — // TODO: Check whether it's a connection issue or the project disabled 3rd-party distribution.
```
                qWarning() << "Failed to cache versions for the current pack!";

            // TODO: Check whether it's a connection issue or the project disabled 3rd-party distribution.
            if (m_current->versionsLoaded && m_ui->versionSelectionBox->count() < 1) {
                m_ui->versionSelectionBox->addItem(tr("No version is available!"), -1);
```

- **TODO** in `launcher/ui/pages/modplatform/flame/FlamePage.cpp`:240 — // TODO: Check whether it's a connection issue or the project disabled 3rd-party distribution.
```
    }

    // TODO: Check whether it's a connection issue or the project disabled 3rd-party distribution.
    if (m_current->versionsLoaded && m_ui->versionSelectionBox->count() < 1) {
        m_ui->versionSelectionBox->addItem(tr("No version is available!"), -1);
```

- **TODO** in `launcher/ui/pages/modplatform/modrinth/ModrinthModel.cpp`:179 — }  // TODO: Move to standalone API
```
            return;
        }
    }  // TODO: Move to standalone API
    ResourceAPI::SortingMethod sort{};
    sort.name = m_currentSort;
```

- **FIXME** in `launcher/ui/dialogs/skins/SkinManageDialog.cpp`:207 — // FIXME: add a model for this, download/refresh the capes on demand
```
void SkinManageDialog::setupCapes()
{
    // FIXME: add a model for this, download/refresh the capes on demand
    auto& accountData = *m_acct->accountData();
    int index = 0;
```

## Unknown (78)

- **FIXME** in `launcher/Application.h`:301 — // FIXME: attach to instances instead.
```
#endif

    // FIXME: attach to instances instead.
    struct InstanceXtras {
        InstanceWindow* window = nullptr;
```

- **todo** in `launcher/FileIgnoreProxy.cpp`:182 — QStack<QModelIndex> todo;
```
            QModelIndex doing = rootIndex;
            int row = 0;
            QStack<QModelIndex> todo;
            while (1) {
                auto node = fsm->index(row, 0, doing);
```

- **todo** in `launcher/FileIgnoreProxy.cpp`:186 — if (!todo.size()) {
```
                auto node = fsm->index(row, 0, doing);
                if (!node.isValid()) {
                    if (!todo.size()) {
                        break;
                    } else {
```

- **todo** in `launcher/FileIgnoreProxy.cpp`:189 — doing = todo.pop();
```
                        break;
                    } else {
                        doing = todo.pop();
                        row = 0;
                        continue;
```

- **todo** in `launcher/FileIgnoreProxy.cpp`:198 — todo.push(node);
```
                {
                    // continue processing cover later
                    todo.push(node);
                } else {
                    // or just block this one.
```

- **todo** in `launcher/FileIgnoreProxy.cpp`:222 — QStack<QModelIndex> todo;
```
        QModelIndex doing = index;
        int row = 0;
        QStack<QModelIndex> todo;
        while (1) {
            auto node = this->index(row, 0, doing);
```

- **todo** in `launcher/FileIgnoreProxy.cpp`:226 — if (!todo.size()) {
```
            auto node = this->index(row, 0, doing);
            if (!node.isValid()) {
                if (!todo.size()) {
                    break;
                } else {
```

- **todo** in `launcher/FileIgnoreProxy.cpp`:229 — doing = todo.pop();
```
                    break;
                } else {
                    doing = todo.pop();
                    row = 0;
                    continue;
```

- **todo** in `launcher/FileIgnoreProxy.cpp`:235 — todo.push(node);
```
            }
            emit dataChanged(node, node, { Qt::CheckStateRole });
            todo.push(node);
            row++;
        }
```

- **TODO** in `tools/generate_todo_report.py`:3 — Scan repository for TODO/FIXME comments and generate a Markdown report.
```
#!/usr/bin/env python3
"""
Scan repository for TODO/FIXME comments and generate a Markdown report.

Usage: python tools/generate_todo_report.py
```

- **TODO** in `tools/generate_todo_report.py`:24 — PATTERN = re.compile(r"\b(TODO|FIXME)\b", re.IGNORECASE)
```
    OUT.parent.mkdir(parents=True, exist_ok=True)

PATTERN = re.compile(r"\b(TODO|FIXME)\b", re.IGNORECASE)

def is_text_file(path: Path) -> bool:
```

- **TODO** in `tools/generate_todo_report.py`:103 — f.write('# TODO/FIXME Report\n\n')
```

    with open(OUT, 'w', encoding='utf-8') as f:
        f.write('# TODO/FIXME Report\n\n')
        f.write('Generated by `tools/generate_todo_report.py`.\n\n')
        f.write('Summary:\n\n')
```

- **TODO** in `cmake/GetGitRevisionDescription.cmake`:193 — # TODO sanitize
```
    endif()

    # TODO sanitize
    #if((${ARGN}" MATCHES "&&") OR
    #	(ARGN MATCHES "||") OR
```

- **TODO** in `cmake/CompilerWarnings.cmake`:124 — # TODO support Intel compiler
```
  else()
    message(AUTHOR_WARNING "No compiler warnings set for CXX compiler: '${CMAKE_CXX_COMPILER_ID}'")
    # TODO support Intel compiler
  endif()

```

- **TODO** in `old/ci/code-quality.nix`:42 — todoCount=$(grep -r "TODO\\|FIXME" ${sourceFiles} --include="*.cpp" --include="*.h" 2>/dev/null | wc -l)
```
    echo "Checking for common code issues..."

    todoCount=$(grep -r "TODO\\|FIXME" ${sourceFiles} --include="*.cpp" --include="*.h" 2>/dev/null | wc -l)
    echo "Found $todoCount TODO/FIXME comments"

```

- **TODO** in `old/ci/code-quality.nix`:43 — echo "Found $todoCount TODO/FIXME comments"
```

    todoCount=$(grep -r "TODO\\|FIXME" ${sourceFiles} --include="*.cpp" --include="*.h" 2>/dev/null | wc -l)
    echo "Found $todoCount TODO/FIXME comments"

    if grep -r "qDebug\\|std::cout" ${sourceFiles} --include="*.cpp" 2>/dev/null | grep -v "// DEBUG" > /dev/null; then
```

- **TODO** in `old/.github/workflows/check.yml`:164 — # Check for TODO/FIXME
```
          fi
          
          # Check for TODO/FIXME
          todo_count=0
          while IFS= read -r file; do
```

- **TODO** in `old/.github/workflows/check.yml`:168 — count=$(grep -c "TODO\|FIXME" "$file" 2>/dev/null || echo 0)
```
          while IFS= read -r file; do
            if [[ -f "$file" ]]; then
              count=$(grep -c "TODO\|FIXME" "$file" 2>/dev/null || echo 0)
              if [ "$count" -gt 0 ]; then
                echo "Info: $file has $count TODO/FIXME comments"
```

- **TODO** in `old/.github/workflows/check.yml`:170 — echo "Info: $file has $count TODO/FIXME comments"
```
              count=$(grep -c "TODO\|FIXME" "$file" 2>/dev/null || echo 0)
              if [ "$count" -gt 0 ]; then
                echo "Info: $file has $count TODO/FIXME comments"
                ((todo_count+=count)) || true
              fi
```

- **TODO** in `old/.github/workflows/check.yml`:179 — echo "  Total TODO/FIXME comments: $todo_count"
```
          echo "Summary:"
          echo "  Files with debug statements: $debug_count"
          echo "  Total TODO/FIXME comments: $todo_count"
          echo ""
          echo "=== Code Quality Check Complete ==="
```

- **fixme** in `old/.github/codeql/codeql-config.yml`:8 — id: cpp/fixme-comment
```
query-filters:
  - exclude:
      id: cpp/fixme-comment

paths-ignore:
```

- **TODO** in `meta/model/mojang.py`:289 — logging: Optional[Dict[str, MojangLogging]]  # TODO improve this?
```
    type: Optional[str]
    inherits_from: Optional[str] = Field("inheritsFrom")
    logging: Optional[Dict[str, MojangLogging]]  # TODO improve this?
    compliance_level: Optional[int] = Field(alias="complianceLevel")
    javaVersion: Optional[JavaVersion]
```

- **FIXME** in `meta/run/generate_mojang.py`:401 — # FIXME: workaround for insane log4j nonsense from December 2021. Probably needs adjustment.
```
                    bucket.libraries.append(lib)
                    bucket.release_time = v.release_time
            # FIXME: workaround for insane log4j nonsense from December 2021. Probably needs adjustment.
            elif lib.name.is_log4j():
                version_override, maven_override = map_log4j_artifact(lib.name.version)
```

- **TODO** in `website/news/release-0.0.3.md`:26 — - **generate_todo_report.py** script for automated TODO scanning and reporting.
```
  - Instance list backup indicators  
- **New CMakePresets.json** replacing multiple per-platform preset files, simplifying the build pipeline.  
- **generate_todo_report.py** script for automated TODO scanning and reporting.  
- **APPLE_SILICON_RATIONALE.md** explaining support considerations and technical details for Apple Silicon.

```

- **FIXME** in `launcher/net/NetRequest.cpp`:246 — * FIXME: report Qt bug for this
```
        /*
         * Next, make sure the URL is parsed in tolerant mode. Qt doesn't parse the location header in tolerant mode, which causes issues.
         * FIXME: report Qt bug for this
         */
        redirect = QUrl(redirectStr, QUrl::TolerantMode);
```

- **FIXME** in `launcher/meta/VersionList.cpp`:204 — // FIXME: this is dumb, we have 'recommended' as part of the metadata already...
```
    }

    // FIXME: this is dumb, we have 'recommended' as part of the metadata already...
    auto recommendedIt =
        std::find_if(m_versions.constBegin(), m_versions.constEnd(), [](const Version::Ptr& ptr) { return ptr->type() == "release"; });
```

- **FIXME** in `launcher/meta/VersionList.cpp`:226 — // FIXME: this is dumb, we have 'recommended' as part of the metadata already...
```
}

// FIXME: this is dumb, we have 'recommended' as part of the metadata already...
static const Meta::Version::Ptr& getBetterVersion(const Meta::Version::Ptr& a, const Meta::Version::Ptr& b)
{
```

- **FIXME** in `launcher/modplatform/EnsureMetadataTask.h`:51 — // FIXME: Move to their own namespace
```

   private:
    // FIXME: Move to their own namespace
    Task::Ptr modrinthVersionsTask();
    Task::Ptr modrinthProjectsTask();
```

- **FIXME** in `launcher/minecraft/AssetsUtils.h`:63 — /// FIXME: this is absolutely horrendous. REDO!!!!
```
};

/// FIXME: this is absolutely horrendous. REDO!!!!
namespace AssetsUtils {
bool loadAssetsIndexJson(const QString& id, const QString& file, AssetsIndex& index);
```

- **FIXME** in `launcher/minecraft/MinecraftInstance.h`:86 — // FIXME: remove
```
    void loadSpecificSettings() override;

    // FIXME: remove
    QString typeName() const override;
    // FIXME: remove
```

- **FIXME** in `launcher/minecraft/MinecraftInstance.h`:88 — // FIXME: remove
```
    // FIXME: remove
    QString typeName() const override;
    // FIXME: remove
    QSet<QString> traits() const override;

```

- **FIXME** in `launcher/minecraft/MinecraftInstance.h`:169 — // FIXME: remove
```
    QString getStatusbarDescription() override;

    // FIXME: remove
    virtual QStringList getClassPath();
    // FIXME: remove
```

- **FIXME** in `launcher/minecraft/MinecraftInstance.h`:171 — // FIXME: remove
```
    // FIXME: remove
    virtual QStringList getClassPath();
    // FIXME: remove
    virtual QStringList getNativeJars();
    // FIXME: remove
```

- **FIXME** in `launcher/minecraft/MinecraftInstance.h`:173 — // FIXME: remove
```
    // FIXME: remove
    virtual QStringList getNativeJars();
    // FIXME: remove
    virtual QString getMainClass() const;

```

- **FIXME** in `launcher/minecraft/MinecraftInstance.h`:176 — // FIXME: remove
```
    virtual QString getMainClass() const;

    // FIXME: remove
    virtual QStringList processMinecraftArgs(AuthSessionPtr account, MinecraftTarget::Ptr targetToJoin) const;

```

- **FIXME** in `launcher/minecraft/Component.cpp`:132 — // FIXME: what if the metadata index isn't loaded yet?
```
std::shared_ptr<class Meta::VersionList> Component::getVersionList() const
{
    // FIXME: what if the metadata index isn't loaded yet?
    if (APPLICATION->metadataIndex()->hasUid(m_uid)) {
        return APPLICATION->metadataIndex()->get(m_uid);
```

- **FIXME** in `launcher/minecraft/Component.cpp`:188 — // FIXME: fake
```
        return vfile->releaseTime;
    }
    // FIXME: fake
    return QDateTime::currentDateTime();
}
```

- **FIXME** in `launcher/minecraft/Component.cpp`:363 — // FIXME: get rid of this try-catch.
```
        return false;
    }
    // FIXME: get rid of this try-catch.
    try {
        QSaveFile jsonFile(filename);
```

- **TODO** in `launcher/minecraft/PackProfile.cpp`:157 — // TODO @RESILIENCE: ignore invalid values/structure here?
```

    // cached
    // TODO @RESILIENCE: ignore invalid values/structure here?
    component->m_cachedVersion = Json::ensureString(obj.value("cachedVersion"));
    component->m_cachedName = Json::ensureString(obj.value("cachedName"));
```

- **FIXME** in `launcher/minecraft/PackProfile.cpp`:320 — // FIXME: actually use fine-grained updates, not this...
```
        return result;
    }
    // FIXME: actually use fine-grained updates, not this...
    beginResetModel();
    // disconnect all the old components
```

- **FIXME** in `launcher/minecraft/PackProfile.cpp`:608 — // FIXME: zero precision mess
```
}

// FIXME: zero precision mess
Qt::ItemFlags PackProfile::flags(const QModelIndex& index) const
{
```

- **FIXME** in `launcher/minecraft/PackProfile.cpp`:680 — // FIXME: get rid of _internal
```
void PackProfile::installJarMods(QStringList selectedFiles)
{
    // FIXME: get rid of _internal
    installJarMods_internal(selectedFiles);
}
```

- **FIXME** in `launcher/minecraft/PackProfile.cpp`:686 — // FIXME: get rid of _internal
```
void PackProfile::installCustomJar(QString selectedFile)
{
    // FIXME: get rid of _internal
    installCustomJar_internal(selectedFile);
}
```

- **FIXME** in `launcher/minecraft/PackProfile.cpp`:721 — // FIXME: get rid of _internal
```
void PackProfile::installAgents(QStringList selectedFiles)
{
    // FIXME: get rid of _internal
    installAgents_internal(selectedFiles);
}
```

- **FIXME** in `launcher/minecraft/PackProfile.cpp`:905 — // FIXME code duplication
```
bool PackProfile::installAgents_internal(QStringList filepaths)
{
    // FIXME code duplication
    const QString patchDir = FS::PathCombine(d->m_instance->instanceRoot(), "patches");
    if (!FS::ensureFolderPathExists(patchDir))
```

- **FIXME** in `launcher/minecraft/OneSixVersionFormat.cpp`:126 — // FIXME: evaluate if we don't want to throw exceptions here instead
```
            out->order = requireInteger(root.value("order"));
        } else {
            // FIXME: evaluate if we don't want to throw exceptions here instead
            qCritical() << filename << "doesn't contain an order field";
        }
```

- **FIXME** in `launcher/minecraft/ComponentUpdateTask.cpp`:137 — // FIXME: dead code. determine if this can still be useful?
```
}

// FIXME: dead code. determine if this can still be useful?
/*
static LoadResult loadPackProfile(ComponentPtr component, Task::Ptr& loadTask, Net::Mode netmode)
```

- **FIXME** in `launcher/minecraft/ComponentUpdateTask.cpp`:179 — // FIXME: to do this right, we need to load the lists and decide on which versions to use during dependency resolution. For now,
```
        RemoteLoadStatus::Type loadType;
        component->resetComponentProblems();
        // FIXME: to do this right, we need to load the lists and decide on which versions to use during dependency resolution. For now,
        // ignore all that...
#if 0
```

- **FIXME** in `launcher/minecraft/ComponentUpdateTask.cpp`:263 — // FIXME: mark error as explicit version conflict
```
        out.equalsVersion = a.equalsVersion;
    } else {
        // FIXME: mark error as explicit version conflict
        return { false, out };
    }
```

- **FIXME** in `launcher/translations/TranslationsModel.cpp`:503 — // FIXME: this is likely never present. FIX IT.
```
    // otherwise install new translations
    bool successful = false;
    // FIXME: this is likely never present. FIX IT.
    d->m_qt_translator.reset(new QTranslator());
    if (d->m_qt_translator->load("qt_" + langCode, QLibraryInfo::path(QLibraryInfo::TranslationsPath))) {
```

- **FIXME** in `launcher/minecraft/auth/MinecraftAccount.cpp`:291 — // FIXME: we now need a better way to identify accounts...
```
    if (!isInUse()) {
        emit changed();
        // FIXME: we now need a better way to identify accounts...
        qWarning() << "Profile" << data.profileId() << "is no longer in use.";
    }
```

- **FIXME** in `launcher/minecraft/auth/MinecraftAccount.cpp`:302 — // FIXME: we now need a better way to identify accounts...
```
    if (!wasInUse) {
        emit changed();
        // FIXME: we now need a better way to identify accounts...
        qWarning() << "Profile" << data.profileId() << "is now in use.";
    }
```

- **FIXME** in `launcher/minecraft/launch/LauncherPartLaunch.cpp`:214 — // FIXME: make this work again
```
                return;
            }
            // FIXME: make this work again
            //  m_postlaunchprocess.processEnvironment().insert("INST_EXITCODE", QString(exitCode));
            //  run post-exit
```

- **FIXME** in `launcher/minecraft/launch/PrintInstanceInfo.cpp`:59 — // FIXME: fixed size buffers...
```
void runLspci(QStringList& log)
{
    // FIXME: fixed size buffers...
    char buff[512];
    int gpuline = -1;
```

- **FIXME** in `launcher/minecraft/launch/PrintInstanceInfo.cpp`:119 — // FIXME: fixed size buffers...
```
void runGlxinfo(QStringList& log)
{
    // FIXME: fixed size buffers...
    char buff[512];
    FILE* glxinfo = popen("glxinfo", "r");
```

- **FIXME** in `launcher/minecraft/mod/ResourceFolderModel.cpp`:476 — // Sadece kopyalama destekleniyor, taşıma (move) desteği yok. FIXME kaldırıldı.
```
                continue;
            }
            // Sadece kopyalama destekleniyor, taşıma (move) desteği yok. FIXME kaldırıldı.
            if (!installResource(url.toLocalFile())) {
                qWarning() << "Failed to install resource from" << url.toLocalFile();
```

- **FIXME** in `launcher/minecraft/mod/ModDetails.h`:78 — // FIXME: come up with a better license parsing.
```
    ModLicense(const QString license)
    {
        // FIXME: come up with a better license parsing.
        // handle SPDX identifiers? https://spdx.org/licenses/
        auto parts = license.split(' ');
```

- **FIXME** in `launcher/modplatform/flame/FlameInstanceCreationTask.cpp`:115 — // FIXME: Aynı modpack için birden fazla kurulum varsa nasıl yönetileceği belirlenmeli.
```
    auto instance_list = APPLICATION->instances();

    // FIXME: Aynı modpack için birden fazla kurulum varsa nasıl yönetileceği belirlenmeli.
    InstancePtr inst;
    if (auto original_id = originalInstanceID(); !original_id.isEmpty()) {
```

- **FIXME** in `launcher/modplatform/flame/FlameAPI.h`:174 — // FIXME: Client-side version filtering. This won't take into account any user-selected filtering.
```
            return arr;
        }
        // FIXME: Client-side version filtering. This won't take into account any user-selected filtering.
        auto const& mc_versions = arr.mcVersion;

```

- **FIXME** in `launcher/modplatform/modrinth/ModrinthInstanceCreationTask.cpp`:66 — // FIXME: Aynı modpack için birden fazla kurulum varsa nasıl yönetileceği belirlenmeli.
```
    auto instance_list = APPLICATION->instances();

    // FIXME: Aynı modpack için birden fazla kurulum varsa nasıl yönetileceği belirlenmeli.
    InstancePtr inst;
    if (auto original_id = originalInstanceID(); !original_id.isEmpty()) {
```

- **FIXME** in `launcher/ui/dialogs/ResourceUpdateDialog.cpp`:265 — // FIXME: Find a more efficient way of doing this!
```
        m_noUpdates = true;
    } else {
        // FIXME: Find a more efficient way of doing this!

        // Sort major items in alphabetical order (also sorts the children unfortunately)
```

- **FIXME** in `launcher/ui/dialogs/ProfileSelectDialog.cpp`:59 — // FIXME: use a real model, not this
```
    view->setColumnCount(1);
    view->setRootIsDecorated(false);
    // FIXME: use a real model, not this
    if (QTreeWidgetItem* header = view->headerItem()) {
        header->setText(0, tr("Name"));
```

- **FIXME** in `launcher/ui/dialogs/VersionSelectDialog.cpp`:119 — // FIXME: overrides custom title given in constructor!
```
void VersionSelectDialog::retranslate()
{
    // FIXME: overrides custom title given in constructor!
    setWindowTitle(tr("Choose Version"));
    m_refreshButton->setToolTip(tr("Reloads the version list."));
```

- **TODO** in `launcher/ui/widgets/MinecraftSettingsWidget.cpp`:509 — else  // TODO select tab
```
    if (id == "javaPage")
        APPLICATION->ShowGlobalSettings(this, "java-settings");
    else  // TODO select tab
        APPLICATION->ShowGlobalSettings(this, "minecraft-settings");
}
```

- **FIXME** in `launcher/ui/widgets/PageContainer.cpp`:175 — // FIXME: unhandled corner case: what to do when there's no page to select?
```
            m_pageList->setCurrentIndex(index);
        } else {
            // FIXME: unhandled corner case: what to do when there's no page to select?
        }
    }
```

- **FIXME** in `launcher/ui/instanceview/InstanceDelegate.cpp`:171 — // FIXME: Bu kodun bağımlılıkları dışarıdan enjekte edilmeli. Şu anda doğrudan erişim var.
```
                return;
            }
            // FIXME: Bu kodun bağımlılıkları dışarıdan enjekte edilmeli. Şu anda doğrudan erişim var.
            auto icon = QIcon::fromTheme(it.next());
            // opt.icon.paint(painter, iconbox, Qt::AlignCenter, mode, state);
```

- **FIXME** in `launcher/ui/instanceview/InstanceView.cpp`:319 — // FIXME: better!
```
    if (index.isValid() && (index.flags() & Qt::ItemIsEnabled)) {
        if (index != currentIndex()) {
            // FIXME: better!
            m_currentCursorColumn = -1;
        }
```

- **FIXME** in `launcher/ui/instanceview/AccessibleInstanceView.cpp`:386 — // FIXME: if indexPosition < 0 in one coordinate, return header
```
    QPoint viewportOffset = view()->viewport()->mapTo(view(), QPoint(0, 0));
    QPoint indexPosition = view()->mapFromGlobal(QPoint(x, y) - viewportOffset);
    // FIXME: if indexPosition < 0 in one coordinate, return header

    QModelIndex index = view()->indexAt(indexPosition);
```

- **FIXME** in `launcher/ui/instanceview/InstanceProxyModel.cpp`:50 — // FIXME: use loaded translation as source of locale instead, hook this up to translation changes
```
    m_naturalSort.setNumericMode(true);
    m_naturalSort.setCaseSensitivity(Qt::CaseSensitivity::CaseInsensitive);
    // FIXME: use loaded translation as source of locale instead, hook this up to translation changes
    m_naturalSort.setLocale(QLocale::system());
}
```

- **FIXME** in `launcher/ui/instanceview/InstanceProxyModel.cpp`:70 — // FIXME: real group sorting happens in InstanceView::updateGeometries(), see LocaleString
```
        return subSortLessThan(left, right);
    } else {
        // FIXME: real group sorting happens in InstanceView::updateGeometries(), see LocaleString
        auto result = leftCategory.localeAwareCompare(rightCategory);
        if (result == 0) {
```

- **FIXME** in `launcher/ui/pages/modplatform/ImportPage.cpp`:132 — // FIXME: actually do some validation of what's inside here... this is fake AF
```
        auto url = QUrl::fromUserInput(input);
        if (url.isLocalFile()) {
            // FIXME: actually do some validation of what's inside here... this is fake AF
            QFileInfo fi(input);

```

- **FIXME** in `launcher/ui/pages/instance/ScreenshotsPage.cpp`:203 — // FIXME: this is a workaround for a bug in QFileSystemModel, where it doesn't
```
        if (role != Qt::EditRole)
            return false;
        // FIXME: this is a workaround for a bug in QFileSystemModel, where it doesn't
        // sort after renames
        {
```

- **FIXME** in `launcher/ui/pages/instance/LogPage.cpp`:265 — // FIXME: turn this into a proper task and move the upload logic out of GuiUtil!
```
        return;

    // FIXME: turn this into a proper task and move the upload logic out of GuiUtil!
    m_model->append(MessageLevel::Launcher,
                    QString("Log upload triggered at: %1").arg(QDateTime::currentDateTime().toString(Qt::RFC2822Date)));
```

- **FIXME** in `launcher/ui/pages/global/LauncherPage.cpp`:82 — // FIXME: possibly move elsewhere
```
#include <QProcess>

// FIXME: possibly move elsewhere
enum InstSortMode {
    // Sort alphabetically by name.
```

- **fixme** in `launcher/ui/pages/modplatform/atlauncher/AtlOptionalModDialog.cpp`:215 — // fixme: plumb in an error message
```

    if (response.error) {
        // fixme: plumb in an error message
        qWarning() << "ATLauncher API Response Error" << response.message;
        return;
```

- **FIXME** in `launcher/ui/pages/modplatform/atlauncher/AtlOptionalModDialog.cpp`:220 — // FIXME: verify pack and version, error if not matching.
```
    }

    // FIXME: verify pack and version, error if not matching.

    // Clear the current selection
```

- **fixme** in `launcher/ui/pages/modplatform/atlauncher/AtlOptionalModDialog.cpp`:239 — // fixme: plumb in an error message
```
    m_jobPtr.reset();

    // fixme: plumb in an error message
}

```

- **fixme** in `launcher/ui/pages/modplatform/atlauncher/AtlOptionalModDialog.cpp`:268 — // fixme: avoid casting here
```
        auto message = QString("%1<br><br>%2").arg(m_version.warnings[mod.warning], tr("Are you sure that you want to enable this mod?"));

        // fixme: avoid casting here
        auto result = QMessageBox::warning((QWidget*)this->parent(), tr("Warning"), message, QMessageBox::Yes | QMessageBox::No);
        if (result != QMessageBox::Yes) {
```


---

Notes:
- `trivial`: likely simple fixes (typos, docs).
- `implementable`: small code changes may resolve.
- `complex`: needs design review or larger refactor.
- `unknown`: manual review recommended.
